rootProject.name = 'FastAsyncWorldEdit'

// This fork is intentionally Bukkit-only. Other platform modules remain
// outside the build so their source and dependencies cannot affect packaging.
include 'core', 'bukkit'
